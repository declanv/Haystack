require 'rails_helper'

RSpec.describe MapsController, :type => :controller do
  # describe 'GET index route' do
  #   before :each do
  #     @user_with_map = FactoryGirl.create(:user_with_map)
  #   end

  #   it "returns  " do
  #     expect{ post :create, @new_user }.to change(User,:count).by(1)
  #   end
  # end

  # get, id = user_with_map.id
  # maps_with_pins = Maps.find(params[:id])

#   n your Thing model

# def as_json(options={})
#   super(:include => :photos)
# end
# And then you can write in your controller just

# render :json => @things
end
