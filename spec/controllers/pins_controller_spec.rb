require "rails_helper"

describe PinsController do
  # describe 'DELETE destroy' do
  #   before :each do
  #     @playlist = FactoryGirl.create(:playlist_with_song)
  #   end

  #   it "deletes the song" do
  #     expect{ delete :destroy, id: @playlist.songs[0].id }.to change(Song,:count).by(-1)
  #   end

  #   it "redirects to playlists#index" do
  #     delete :destroy, id: @playlist.songs[0].id
  #     response.should redirect_to "/"
  #   end
  # end

  describe 'POST create' do

  end

end
